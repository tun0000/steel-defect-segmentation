# secrets
.env

# python
__pycache__/
*.pyc
.venv/

# data & artifacts — never commit datasets, weights, or training runs
data/
datasets/
weights/
runs/
*.pt
*.onnx
*.engine
*.zip

# notebooks
.ipynb_checkpoints/

# local dev tooling config (personal paths, not portable)
.claude/
